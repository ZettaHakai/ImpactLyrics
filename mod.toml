# Fields that end up in the mod's manifest.
[manifest]

# Unique ID of this mod. Pick something long enough that it'll never be the same as any other mod.
# The name displayed in the mod management menu is separate from this so this doesn't need to be human readable.
id = "impact"

# Version of this mod.
version = "1.0.2"

# The name that will show up for this mod in the mod menu. This should be human readable.
display_name = "Impact Lyrics."

# The description that will show up when this mod is displayed in the mod menu. This should be human readable.
description =  """
This is a mod that replaces the lyrics of I am Impact! with Romaji Japan stylized, Romaji English stylized, The Goemon International Translation, the Direct Translation of lyrics, or no lyrics.  Special Thanks to the Goemon Discord Community for help in improving this mod."""

# A short description that will show up in this mod's entry in the mod list. This should be human readable and kept short
# to prevent it from being cut off due to the limited space.
short_description = "Fixes I am Impact Lyrics"

# Authors of this mod.
authors = [ "Zetta_Hakai, with thanks to Klorfmorf, KillKlli, abyssonym, and Goshen" ]

# ID of the target recomp game.
game_id = "mnsg"

# Minimum version of the target recomp (e.g. Goemon 64: Recompiled) that this mod can run on.
minimum_recomp_version = "0.1.0"

# Dependency mods. Each entry is the mod's ID and then an optional minimum version of the dependency mod.
dependencies = [
# Example dependency:
#    "modname:1.0.0"
]

# Native libraries (e.g. DLLs) and the functions they export.
native_libraries = [
# Example native library:
#    { name = "my_native_library", funcs = ["my_native_library_function"] }
]

# Inputs to the mod tool.
[inputs]

# Input elf file to generate a mod from.
elf_path = "build/mod.elf"

# Output mod filename.
mod_filename = "ImpactLyrics"

# Reference symbol files.
func_reference_syms_file = "Goemon64RecompSyms/mnsg.us.syms.toml"
data_reference_syms_files = [ "Goemon64RecompSyms/mnsg.us.datasyms.toml" ]

# Additional files to include in the mod.
additional_files = [ ]

[[manifest.config_options]]
id = "impact_lyrics_version"
name = "Impact Lyrics Version"
description = "Choose which lyric version to load for the Impact scenario."
type = "Enum"
options = ["Romaji (J)", "Romaji (E)", "GoeInt",  "Translated", "Removed"]
default = "Romaji (J)"
